# Secrets
.env
.env.*
!.env.example
credentials
*.pem
*.key

# Java
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar
.mvn/

# Node / Next.js
node_modules/
.next/
out/
.vercel/

# OS / editor
.DS_Store
Thumbs.db
.idea/
*.iml
.vscode/*
!.vscode/extensions.json

# Build artifacts
*.log

# Supabase CLI local cache
supabase/.temp/
supabase/.branches/

# Source photographs supplied for Phase 4 seeding — not ours to redistribute
# via the repo (see BUILD_LOG.md, Phase 4 4.1 entry). Already uploaded through
# the real image pipeline to Supabase Storage for the products that use them.
clothes/

# Project-governance docs — kept locally for reference, deliberately excluded
# from the GitHub repo itself (removed from history 2026-08-21, per request).
/CLAUDE.md
/docs/PRD.md
/docs/OBJECTIVES.md